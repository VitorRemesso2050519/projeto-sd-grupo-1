apiVersion: v1
kind: Service
metadata:
  name: simulator-metrics
  namespace: grupo1
  labels:
    app: simulator
spec:
  type: ClusterIP
  ports:
    - name: metrics-simulator
      port: 8080
      targetPort: 8080
      protocol: TCP