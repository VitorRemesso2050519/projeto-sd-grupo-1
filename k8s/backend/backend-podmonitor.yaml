apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: backend-podmonitor
  namespace: grupo1
  labels:
    app: backend
spec:
  selector:
    matchLabels:
      app: backend
  podMetricsEndpoints:
    - port: 8000
      path: /metrics
      interval: 15s
